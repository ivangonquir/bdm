verything is implemented. Here's a summary of what was created:

What was built
New services (docker-compose.yml)
Service	Purpose	Port
ClickHouse	Structured data (trusted + exploitation)	8123
MongoDB	Semi-structured data (trusted + exploitation)	27017
etcd	Required by Milvus	—
Milvus	Vector embeddings	19530
Airflow now builds from airflow/Dockerfile (pre-installs all packages including fastembed) instead of using slow runtime pip installs.

Trusted Zone (trusted-zone/)
Script	What it does
clean_noaa.py	Drops nulls, converts tenths→°C, validates range, deduplicates → ClickHouse trusted.noaa_bcn
clean_openweather.py	Deduplicates on event_ts, validates temp/humidity ranges → MongoDB trusted.weather_stream
clean_eltiempo.py	Validates HTML structure, re-encodes UTF-8 → MinIO trusted-zone
clean_satellite.py	Validates PNG magic bytes, minimum size → MinIO trusted-zone
Exploitation Zone (exploitation-zone/)
Script	What it does
build_temperature_unified.py	Joins NOAA + OpenWeather into one denormalised table → ClickHouse exploitation.temperature_unified
compute_kpis.py	Monthly and seasonal avg/min/max KPIs → ClickHouse exploitation.temperature_kpis
curate_weather.py	Adds season, comfort_index, is_extreme → MongoDB exploitation.weather_curated
organize_unstructured.py	Server-side copy trusted→exploitation → MinIO exploitation-zone
compute_embeddings.py	Extracts HTML text, generates 384-dim embeddings → Milvus eltiempo_embeddings
DAG (airflow/dags/ingestion-dag.py)
Full end-to-end pipeline: ingestion → landing → trusted → exploitation, all orchestrated with correct task depe